spring:
  application:
    name: allocation-service

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
  instance:
    instance-id: ${spring.application.name}-${random.int}

server:
  port: 0